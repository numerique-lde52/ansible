---
# file copy
# see https://docs.ansible.com/ansible-core/2.12/collections/ansible/builtin/copy_module.html#ansible-collections-ansible-builtin-copy-module

- name: My task
  hosts: tplink_dev
  tasks:
    - name: Leaving a mark
      command: "touch /tmp/ansible_was_here"

    - name: Ensure the httpd service is running
      service:
        name: httpd
        state: started
      become: yes
      become_user: ligue15
